import XCTest

class ColorViewControllerTests: XCTestCase {
    
    override func setUp() {
        super.setUp()
        // setup a bunch of ColorViewController's here with a bunch of different paramerters (different set of colors; material-primary, material-secondary, just one or two, thousands, many of the same color 
    }
    
    override func tearDown() {
        super.tearDown()
    }
    
    func testNumberOfCellsEqualsProvidedColorsCount() {
        XCTAssertTrue(true)
    }
}